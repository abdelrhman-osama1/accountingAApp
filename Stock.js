import React from "react"
import { Button,View, FlatList, platform, AppRegistry, Text, SafeAreaView, StyleSheet, ImageBackground, Pressable, Dimensions,Alert, TextInput } from 'react-native'

const Stock = ({ navigation }) => {
    return(
        <View>
            <Text> hello</Text>
        </View>
    )
}

export default Stock